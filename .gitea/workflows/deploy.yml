name: CI/CD to Gitea

on:
  push:
    branches: [main, master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        run: |
          sshpass -p "123quant-speed" ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 "quant@6.6.6.66" '
            cd /home/quant/data/dev/Web_Admin_UI &&
            git pull https://jeremygan2021:Ganqweasdzxc1%21@gitea.tangledup-ai.com/quantclaw/Web_Admin_UI.git main &&
            docker compose build --no-cache app &&
            docker compose up -d --force-recreate app &&
            docker logs --tail 30 quantclaw_app
          '